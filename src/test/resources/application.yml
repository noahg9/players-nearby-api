spring:
  application:
    name: pickup-sports
  mvc:
    problemdetails:
      enabled: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  # datasource is NOT set here — @ServiceConnection on TestContainers overrides it automatically

server:
  port: 8080

app:
  base-url: http://localhost:3000
  jwt-secret: test-secret-for-unit-tests
  resend-api-key: ""
